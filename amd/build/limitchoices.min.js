define(["jquery"], function (d) {
	var b,
		e = function () {
			var c = [],
				a = []
			d(".answer input[type=checkbox]").each(function (f) {
				this.checked ? a.push(this) : c.push(this)
			})
			if (a.length > b) for (; a.length > b; ) a.pop().checked = !1
			d(c).each(function (f) {
				this.disabled = a.length == b
			})
		}
	return {
		init: function (c) {
			b = c
			e()
			d(".answer input[type=checkbox]").each(function (a) {
				d(this).on("change", e)
			})
		},
	}
})
