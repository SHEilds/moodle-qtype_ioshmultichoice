define(["jquery"], function (b) {
	var d,
		e = function () {
			var c = [],
				a = []
			for (
				b(".answer input[type=checkbox]").each(function (f) {
					this.checked ? a.push(this) : c.push(this)
				});
				a.length > d;

			)
				a.pop().checked = !1
			b(c).each(function (f) {
				this.disabled = a.length == d
			})
		}
	return {
		init: function (c) {
			d = c
			e()
			b(".answer input[type=checkbox]").each(function (a) {
				b(this).on("change", e)
			})
		},
	}
})
